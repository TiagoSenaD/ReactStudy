import style from './Footer.module.css';

const Footer = () => {
    return (
        <footer className={style.footer}>
            <p>Definitivamente um footer.</p>
        </footer>
    );
};

export default Footer;